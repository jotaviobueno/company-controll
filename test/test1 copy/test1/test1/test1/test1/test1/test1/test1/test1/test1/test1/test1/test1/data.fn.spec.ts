import { data } from './data.fn';

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});

describe('Data', () => {
  it('calculates final price correctly with discount', () => {
    const result = data(1);

    expect(result).toBe(2);
  });
});
